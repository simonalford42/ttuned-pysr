{
  "name": "onestep-s",
  "seed": 42,
  "data_dir": "data",
  "train_file": "harder_problems_one_step_train.jsonl",
  "val_file": "harder_problems_one_step_val.jsonl",
  "model_config": "training/gpt-neo-s.json",
  "num_train": -1,
  "num_val": -1,
  "context_length": 4096,
  "training_args": {
    "output_dir": "training/checkpoints/onestep-s",
    "overwrite_output_dir": true,
    "num_train_epochs": 1,
    "per_device_train_batch_size": 12,
    "per_device_eval_batch_size": 12,
    "gradient_accumulation_steps": 1,
    "warmup_steps": 50,
    "learning_rate": 1e-5,
    "weight_decay": 0.01,
    "lr_scheduler_type": "cosine",
    "logging_steps": 100,
    "save_steps": 500,
    "eval_steps": 500,
    "evaluation_strategy": "steps",
    "save_strategy": "steps",
    "load_best_model_at_end": true,
    "metric_for_best_model": "eval_loss",
    "greater_is_better": false,
    "dataloader_drop_last": true,
    "remove_unused_columns": false,
    "bf16": true,
    "fp16": false,
    "save_total_limit": 40,
    "max_grad_norm": 1.0
  },
  "wandb": {
    "project": "ttsr",
    "entity": "simon-alford",
    "dir": "./wandb"
  }
}
